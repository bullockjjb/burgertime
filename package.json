{
  "name": "accelevation-time",
  "version": "1.1.0",
  "private": true,
  "description": "A branded, offline-ready data center assembly arcade game.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=20.11"
  },
  "license": "GPL-3.0-or-later"
}
